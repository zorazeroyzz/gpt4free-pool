# gpt4free-pool 环境变量配置

# ============ gpt4free-pool 配置 ============

# API 访问密钥（可选）
# 设置后，所有 /v1/* 请求必须携带 Authorization: Bearer <此密钥>
# 不设置则无需认证（免费访问模式）
API_KEY=

# ============ 联合部署配置 (docker-compose.oneapi.yml) ============

# gpt4free-pool 的 API 密钥（用于 one-api 联合部署时）
# 设置后需在 one-api 添加渠道时使用相同的密钥
FREEAPI_KEY=

# one-api 初始 root 令牌（首次启动时自动创建）
ONEAPI_ROOT_TOKEN=
